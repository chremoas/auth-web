namespace: com.aba.srv
name: authweb
net:
  listenHost: localhost
  listenPort: 3000
oauth:
  clientId: some client id from CCP
  clientSecret: some secret from CCP
  callBackUrl: /sso/callback
debug: false
discord:
  inviteUrl: url to be used for invitations
serviceNames:
  authSrv: auth-srv
